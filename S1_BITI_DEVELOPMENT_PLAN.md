# 📋 S1_BITI_DEVELOPMENT_PLAN.md (비티 1차 개발 계획서) v0.2

* **작성 목적**: 리뷰와이(Reviewai) MVP 설계를 바탕으로 덱스(Dex)의 본격적인 스프린트 2 개발을 지시하고 가이드하기 위해 기술적 아키텍처, 기능 스코프, 화면 플로우 및 QA 기준을 수립한다.
* **작성일**: 2026-06-06
* **대상 페이지**: [AI 리뷰·평판관리 SaaS] 서비스 - 리뷰와이(Reviewai) 개발 (`RAI-S1-ST03`)
* **버전 기록**:
  * v0.1: 최초 작성 (2026-06-05)
  * v0.2: 라이언 2차 피드백 반영 (CSV 한정, Gemini 연동 유예, 화면 라우트 축소, 결제 기능 제외, S2-ST01 범위 수정)
* **준수 사항**: 아직 실제 scaffold 생성이나 덱스 작업 지시는 착수하지 않으며, 라이언의 최종 검토 및 승인을 득한 후 실행한다.

---

## 1. MVP 기능 범위

### 1.1 In-Scope (개발 및 검증 대상)
* **스토어 정보 입력**: 분석 전 사장님의 쇼핑몰 스토어명 및 타깃 카테고리 정보 입력.
* **수동 리뷰 데이터 입력**: 
  * 텍스트 복사-붙여넣기 (textarea 입력을 통한 텍스트 라인 파싱)
  * **CSV 파일 업로드만 지원**: 표준 CSV 템플릿 제공 및 드롭존 UI 구현.
    * 필수 컬럼: `reviewText` (리뷰 본문), `rating` (평점 1~5)
    * 선택 컬럼: `date` (작성일), `options` (선택 옵션)
* **리뷰 100개 제한 분석**: 최대 100개 리뷰까지만 업로드 및 AI 분석을 수행하며, 100개 초과 시 '별도 견적 안내 및 1:1 상담 연결' 팝업/화면 노출.
* **대시보드 요약 정보**: 전체 리뷰 개수, 평균 별점, 위험 리뷰 개수, 반복 불만 TOP 5, 핵심 긍정/부정 키워드 목록.
* **브랜드 톤 기반 답글 초안 (내부 탭/섹션)**: 사장님이 4가지 브랜드 톤 중 하나를 선택하면 AI가 맞춤형 답글 초안 3종 생성.
  * **친절한 사장님형**, **전문 브랜드형**, **담백한 CS형**, **재방문 유도형**
* **민감단어 가드레일 (내부 탭/섹션)**: 5대 위험 표현군 감지 시 자동으로 답글 카드 상단에 **[민감 표현 감지 - 담당자 검수 필요]** 경고 배지 노출 및 수동 편집 유도.
* **상세페이지 및 FAQ 개선 포인트 제안 (내부 탭/섹션)**: 별점 하락이나 불만 리뷰 데이터를 기반으로 상세페이지 문구 보완점 및 FAQ 수정 팁을 리스트로 제공.
* **리포트 미리보기 및 인쇄 레이아웃**: 사장님이 즉시 49,000원 상당의 리포트를 미리보거나, 브라우저 인쇄 기능을 통해 PDF/종이 출력을 깨끗하게 할 수 있는 인쇄 전용 CSS 디자인 지원.
* **월간 관리팩 안내**: 실제 결제 모듈 없이, 월간 리뷰 관리팩 서비스 안내 CTA 영역과 1:1 카카오톡 상담 연결 문구/버튼으로 구현.

### 1.2 Out-of-Scope (제외 범위)
* Excel(`.xlsx`) 파일 업로드 및 분석 (S3 이후 검토).
* API 및 웹 크롤러 기반의 실시간 100% 자동 리뷰 수집.
* 카카오톡 리뷰 수집 챗봇 연동.
* AI 자동 답글 매장 즉시 등록/게시 (항상 사장님 검수 후 수동 복사).
* XAI(설명 가능한 AI) 허위 리뷰 판정 인공지능 모델 개발.
* GEO HTML/JSON 자동 배포 및 도메인 바인딩 시스템.
* PDF 백엔드 자동 변환 및 다운로드 엔진 (S3 이후 검토).
* 실결제 모듈 (S4 이후 결제/신청 연계 단계에서 처리).

---

## 2. 화면 목록
* **홈 / 서비스 소개 (`/`)**: 49,000원 리뷰 진단 리포트 상품 가치 제안 및 '샘플 분석 보기' 버튼 배치.
* **리뷰 데이터 입력 (`/submit`)**: 스토어 정보 입력, 텍스트 복붙 탭 및 CSV 파일 업로드 영역 제공. (URL 입력은 향후 확장 기능으로만 명시하며 수동 입력 유도)
* **분석 진행 / 대기 (`/analysis/progress`)**: AI 분석 동안 로딩률 및 수동 가이드(검수 필수 등) 안내 텍스트 노출.
* **리포트 결과 대시보드 (`/report`)**: 리뷰 분석 결과를 통합하여 단일 라우트 상에서 탭(Tab) 또는 섹션(Section) 분할 구조로 보여줌.
  * **[탭 1] 리뷰 분석 요약**: 리뷰 수, 평균 별점, 별점 하락 우려 신호, 위험 리뷰 리스트, 반복 불만 TOP 5.
  * **[탭 2] 답글 초안 생성**: 4가지 브랜드 톤 탭, 톤별 답글 초안 3종, 가드레일 위반 경고 배지 노출.
  * **[탭 3] 상세페이지/FAQ 개선 가이드**: AI가 추천하는 개선 가이드 카드 및 결제/상담 연결 CTA 버튼 배치. 브라우저 인쇄 친화 레이아웃 적용.

---

## 3. 사용자 플로우
```
[홈페이지 진입] ➔ [리포트 가치 확인] ➔ [리뷰 입력 화면 이동]
       │
       ├─➔ [리뷰 100개 이내 텍스트 복붙 or CSV 업로드] ➔ [분석 실행] ➔ [분석 로딩 뷰] ➔ [결과 리포트 조회 (/report)]
       └─➔ [리뷰 100개 초과 입력] ➔ [100개 초과 제한 팝업 / 견적 안내] ➔ [상담 CTA]
                                                 │
                                 [탭 전환: 요약 / 답글초안 / 개선제안]
                                                 │
                             [리포트 미리보기/인쇄] ➔ [월간 관리팩 상담 신청 CTA]
```

---

## 4. 입력 데이터 구조
```typescript
// 스토어 정보 입력 규격
interface StoreInfo {
  storeName: string;      // 스토어명 (필수)
  category: string;       // 상품 카테고리 (필수)
  productName: string;    // 분석 대상 상품명 (선택)
}

// 리뷰 데이터 행 규격 (.csv)
interface RawReview {
  reviewText: string;     // 리뷰 본문 (필수)
  rating: number;         // 평점 (1~5, 필수)
  date?: string;          // 작성일 (선택)
  options?: string;       // 선택 옵션 (선택)
}
```

---

## 5. 출력 리포트 구조
```typescript
interface ReviewAnalysisReport {
  reportId: string;
  storeName: string;
  timestamp: string;
  summary: {
    totalCount: number;
    averageRating: number;
    negativeCount: number;      // 1~3점 리뷰 수
  };
  topComplaints: Array<{
    complaint: string;          // 반복 불만 내용
    frequency: number;          // 비율 (%)
  }>;
  ratingDropSignals: string[];  // 별점 하락 우려 신호 텍스트
  riskReviews: Array<{
    reviewText: string;
    rating: number;
    detectedRiskType: string;   // 예: "품질하자", "CS대응지연", "환불요구"
    priority: number;           // 우선순위 1~3
  }>;
  replyDrafts: Record<string, string[]>; // 4종 브랜드 톤별 답글 초안 리스트
  improvements: Array<{
    problemArea: string;        // 불만 영역
    actionableTip: string;      // 개선 가이드 텍스트
  }>;
}
```

---

## 6. Mock data 우선 개발 계획
덱스(Dex)가 API 및 프롬프트 연동과 무관하게 독립적으로 화면 및 상태를 구현할 수 있도록 정적 Mock 데이터를 적극 활용한다.
* **샘플 데이터 준비**: `samples/sample_reviews_100.csv` 및 `samples/sample_analysis_mock.json`을 프로젝트 루트에 미리 배치.
* **Mock 분석 기능**: 리뷰 입력 화면에서 "샘플 리뷰 불러오기" 버튼을 누를 시 즉시 Mock JSON 데이터가 메모리에 적재되어 로딩 단계를 거쳐 결과 대시보드(`/report`)로 이동되도록 구현.

---

## 7. AI API 연동 보류/후순위 계획
* **Gemini API 연동 유예**: S2의 핵심 목표는 Mock data 기반 UI/플로우 완성이다. Gemini API 실제 연동은 **S2 후반 선택 구현 또는 S3 기본 검토 항목**으로 이월한다.
* **보안 지침 (프론트엔드 직접 노출 금지)**: 프론트엔드 코드에 API Key를 직접 하드코딩하거나 노출하지 않는다. 
* **중계 서버리스/API 아키텍처 검토**: S3 실제 연동 시점에는 **Vercel Serverless Functions, Firebase Functions, Express API 서버** 중 하나를 중계 플랫폼으로 채택하여 API Key의 노출을 완벽하게 예방한다.

---

## 8. 덱스 작업 분해 수정 (S2 계획)
 docs 폴더 6대 운영 문서 작성은 S1 기획 단계의 최종 산출물로 귀속하며 덱스의 S2 개발 태스크에서 공식 제외한다.
* **`RAI-S2-ST01`**: 프로젝트 scaffold 및 기본 README/폴더 구조 생성 (docs, product, app, data, prompts, .github 디렉토리 정렬)
* **`RAI-S2-ST02`**: 공통 UI 테마 정의 및 신뢰감 있는 블루/그레이 중심 CSS(index.css)와 기본 버튼/카드 레이아웃 컴포넌트 세팅
* **`RAI-S2-ST03`**: 홈 화면 기획 적용 및 스토어 정보 입력, 수동 리뷰 데이터 입력 화면(복붙 + CSV 파일 업로드) 개발
* **`RAI-S2-ST04`**: 리뷰 100개 제한 로직 구현 및 100개 초과 시 '견적 안내 및 1:1 상담 CTA' 팝업 개발
* **`RAI-S2-ST05`**: 분석 대기 진행 뷰 및 통합 대시보드 리포트 화면(`/report`) 마운트 (Mock 데이터 연동)
* **`RAI-S2-ST06`**: 위험 리뷰 리스트 카드 및 별점 하락 우려 신호 분석 요약 컴포넌트 개발
* **`RAI-S2-ST07`**: 4종 브랜드 톤 선택에 따른 AI 답글 초안 컴포넌트 개발 및 클립보드 복사 기능 구현 (탭 전환식 배치)
* **`RAI-S2-ST08`**: 5대 위험 표현군 필터링 및 **[민감 표현 감지 - 수동 검수 필수]** 배지 UI 구현
* **`RAI-S2-ST09`**: 상세페이지/FAQ 개선 포인트 제안 카드 및 브라우저 인쇄 친화형 CSS 스타일(Print styling) 디자인 적용
* **`RAI-S2-ST10`**: 신청/상담 연결 CTA 폼 구현 및 최종 통합 테스트

---

## 9. QA 기준 초안 (PM/QA 관점)
* **입력 한계점 테스트**: 리뷰 데이터가 0개인 경우, 1개인 경우, 100개인 경우, 101개인 경우(100개 초과 경고 정상 노출 여부)를 각각 전수 검증.
* **CSV 파싱 강건성 검증**: 필수 컬럼(`reviewText`, `rating`) 및 선택 컬럼(`date`, `options`) 파싱 및 예외 행 처리 정상화 테스트.
* **가드레일 경고 검증**: 5대 민감 표현군(환불, 보상, 전적 잘못, 완치, 허위 리뷰 등)이 포함된 텍스트 입력 시 경고 배지가 깨지지 않고 즉각 답글 초안 위에 명시되는지 확인.
* **인쇄 레이아웃 및 반응형 확인**: 모바일/태블릿 등 다양한 뷰포트에서 리포트 뷰가 무너지지 않는지 확인하고, 브라우저 인쇄(Ctrl+P) 창을 띄웠을 때 페이지 분할이 깔끔한지 확인.

---

## 10. 빌/라이언 확인 필요 질문
* **Q1. CSV 템플릿 제공 파일 생성**: S2 진입 시, 덱스에게 표준 CSV 형식을 다운로드할 수 있는 샘플 파일 생성 지시(`data/sample_review_template.csv`)를 포함하는 것이 타당한지 승인이 필요합니다.
* **Q2. Vercel Serverless Functions 검토**: 연동 유예된 Gemini API를 추후 S3에 중계할 때, 별도 독립 서버 운영 비용이 들지 않는 Vercel Serverless Functions 방식을 기본 중계 아키텍처로 선호하시는지 확인을 요청드립니다.
