# 📋 S1_DEX_UX_UI_TASK_SPEC.md v0.3 (S2 개발자용 UX/UI 세부 사양서)

본 문서는 `RAI-S1-ST06D — ST08 덱스 작업 분해표에 UX/UI 기준 반영 준비` 백로그의 최종 산출물입니다. 스프린트 2(S2) 단계에서 개발자 덱스(Dex)가 작업을 분해하고 개발 범위를 확인할 때, UX/UI 시각 퀄리티와 검수 조건이 누락되지 않도록 개발자용 가이드와 완료 기준(Acceptance Criteria)을 1:1로 매핑하여 명문화했습니다.

> [!WARNING]
> **주의**: 본 산출물은 실제 앱 코드 작성이나 덱스 개발 착수가 아니라, ST08에서 덱스 작업 분해표를 작성하기 위한 UX/UI 기준 정리 문서입니다. 따라서 scaffold 생성, 앱 코드 작성, PR 생성, 브랜치 생성은 아직 수행하지 않습니다.

---

## 1. 문서 목적

* **ST08 덱스 작업 분해표 작성 기준**: 덱스가 S2 태스크를 쪼개어 스프린트 계획을 수립할 때, 시각적 완성도와 사용성을 확보할 수 있는 분해 가이드라인을 제공합니다.
* **S2 개발 착수 전 UX/UI 인수 조건(AC) 정리**: 각 티켓별로 작동해야 하는 세부 마크업, CSS 속성값, 인터랙션 요건을 명확히 함으로써 개발 편차를 방지합니다.
* **덱스 임의 디자인 방지**: 덱스가 독자적으로 컬러, 여백, 섹션 순서를 바꾸거나 불법/편법적인 UI(자동 수집, 자동 답글 등록 등)를 추가하지 않도록 명확한 규격 가이드를 제시합니다.
* **빌/라이언 QA 기준 연결**: `S1_S2_DESIGN_QA_CHECKLIST.md` v0.3에 명시된 QA 검수 ID를 개발 티켓에 1:1로 바인딩하여 49,000원 상품 수준의 최종 핸드오프 품질을 통제합니다.

---

## 2. 반드시 계승할 승인 문서

본 사양서는 오직 아래 4대 승인 기획 문서에 담긴 기준과 범위만을 상속하여 작성되었습니다.
1. **[S1_UX_UI_REFERENCE_RESEARCH.md](file:///Users/user/Documents/reviewai/S1_UX_UI_REFERENCE_RESEARCH.md) v0.4**
2. **[S1_LANDING_REPORT_DESIGN_DIRECTION.md](file:///Users/user/Documents/reviewai/S1_LANDING_REPORT_DESIGN_DIRECTION.md) v0.4**
3. **[S1_S2_DESIGN_QA_CHECKLIST.md](file:///Users/user/Documents/reviewai/S1_S2_DESIGN_QA_CHECKLIST.md) v0.3**
4. **[S1_PRD_V1_6.md](file:///Users/user/Documents/reviewai/S1_PRD_V1_6.md) v0.3**

---

## 3. S2 UX/UI 5대 태스크 요약

덱스가 S2 백로그로 등록하여 처리할 5대 UX 개발 티켓의 요약입니다.

| 티켓 ID | 백로그명 | 핵심 구현 내용 |
| :--- | :--- | :--- |
| **`RAI-S2-UX01`** | **디자인 토큰/공통 컴포넌트** | CSS 변수 정의, 타이포그래피 계층, 공통 버튼/카드 스타일 및 44px 터치 영역 |
| **`RAI-S2-UX02`** | **랜딩 페이지 구현 및 QA** | 홈 7대 섹션 흐름 마크업, CSV 업로드 드롭존 유효성 피드백, 100건 초과 모달 |
| **`RAI-S2-UX03`** | **리포트 시각 계층 구현 및 QA** | 메인 4대 지표 고정, 부정 키워드 칩 격리, 오늘의 요약 3종, AI 답글 수동 복사 |
| **`RAI-S2-UX04`** | **모바일 탭/리뷰 카드/CTA QA** | 375px/390px 뷰포트 반응형, 가로 스크롤 필 탭, 2줄 아코디언, 플로팅 CTA 배제 |
| **`RAI-S2-UX05`** | **인쇄 레이아웃 QA** | `@media print` 전용 스타일 필터, 배경 화이트/텍스트 딤블랙, 인쇄 마진 최적화 |

---

## 4. 각 태스크별 상세 사양

### 4.1. RAI-S2-UX01 — 디자인 토큰/공통 컴포넌트
* **목적**: 리뷰와이의 일관되고 신뢰감 높은 프리미엄 스타일 구축을 위해 공통 디자인 토큰과 기본 UI 골격을 설계합니다.
* **작업 범위**: `index.css` 내 CSS Custom Properties(디자인 토큰) 작성 및 공통 컴포넌트 마크업 설계.
* **주요 UI 컴포넌트**: 기본 Button, Card Container, Badge/Chip, Modal Backdrop.
* **연결 화면**: 서비스 전반 (`/`, `/submit`, `/analysis/progress`, `/report`).
* **연결 QA ID**: `QA-MB-06` 및 디자인 시스템 관련 토큰 검수 항목.
* **Acceptance Criteria**:
  1. **컬러 토큰 정의**: `Primary` (#1D4ED8), `Background` (#F8FAFC), `Card Background` (#FFFFFF), `Border` (#E2E8F0), `Text Primary` (#0F172A), `Text Secondary` (#64748B) 변수 설정.
  2. **타이포그래피 위계**: 기본 폰트 `Pretendard` 폴백 지정, `H1` (28px/bold), `H2` (20px/semibold), `Body` (15px/regular) 설정.
  3. **둥글기 및 그림자**: 대형 레이어 카드 `border-radius: 12px`, 기본 버튼/인풋 `border-radius: 8px` 적용. 은은한 그림자 효과(`box-shadow`) 기본 탑재.
  4. **터치 타깃 확보**: 모바일 브라우저 오동작 방지를 위해 모든 버튼 및 clickable 영역은 최소 가로/세로 **44px 이상** 영역을 확보할 것.
* **금지 사항**: 기획서에 명시되지 않은 ad-hoc 색상 코드(예: plain red, plain blue)의 코드 내 하드코딩 금지.
* **PR 제출 증빙**: 로컬 확인 URL, `index.css` 파일 구조 및 44px 터치 영역 적용 캡처.

### 4.2. RAI-S2-UX02 — 랜딩 페이지 구현 및 QA
* **목적**: 소상공인 셀러에게 서비스 가치를 자연스럽게 전파하고, 안정적인 CSV 업로드 피드백과 모달 가드레일을 통해 이탈률을 낮춥니다.
* **작업 범위**: `/` 홈 화면, `/submit` 데이터 입력 화면 퍼블리싱 및 클라이언트 파싱 피드백 연동.
* **주요 UI 컴포넌트**: Hero Section, Value Card, CSV Upload Dropzone, Limit Modal, Empty State Card.
* **연결 화면**: `/` 홈 화면, `/submit` 입력 화면.
* **연결 QA ID**: `QA-HM-01~06`, `QA-IP-01~07`.
* **Acceptance Criteria**:
  1. **홈 7대 섹션 흐름**: Header -> Hero -> 가치 카드 -> 샘플 미리보기 -> 추천 사장님 -> 단일 상품 구성 -> 풋터 CTA 순으로 마크업 및 배치.
  2. **히어로 영역 카피**: "약 3분 내외로 정리하는 고객 불만 신호와 브랜드 맞춤형 AI 리뷰 답글 비서" 문안 및 "리뷰 리포트 만들기" Primary CTA 연동.
  3. **CSV 드롭존 설명**: "CSV 파일을 드래그 앤 드롭 하거나 컴퓨터에서 파일 선택" 명시. Excel(.xlsx) 혼동 차단을 위해 CSV 단일 포맷 지원을 직관적으로 표기.
  4. **실시간 피드백**: 파일 업로드 즉시 사용자가 첨부한 파일명(예: `reviews.csv`) 및 파싱 건수(예: `총 85건 파싱 완료`)를 시각적으로 노출.
  5. **유효성 검사**: 필수 컬럼 `reviewText` 및 `rating` 검출 시 녹색 체크 배지 활성화. 누락 시 붉은색 경고 테두리와 함께 파싱 에러 메시지(Empty/Error State) 표출.
  6. **100건 초과 모달**: 101건 이상 파싱 시 100건 초과 Limit Modal 팝업 표출. 내부 중앙에 카카오 공식 노란색(#FEE500) 테마의 1:1 상담 문의 버튼 배치.
  7. **샘플 데이터 연동**: "테스트용 샘플 데이터 불러오기" 클릭 시 즉각 15행의 규격 데이터가 바인딩되고 요약 텍스트 노출.
* **금지 사항**: 크롤링 URL 입력 폼 임의 생성 금지, 자동 리뷰 수집 시뮬레이션 UI 추가 금지, 모바일 풋터 플로팅 결제 바 고정 금지.
* **PR 제출 증빙**: 모바일 SE 및 일반 모바일 375px/390px 캡처, 용량 경계값(0건, 1건, 100건, 101건) 테스트 결과 로그.

### 4.3. RAI-S2-UX03 — 리포트 시각 계층 구현 및 QA
* **목적**: 49,000원 상당의 유료 진단서에 걸맞은 정보 밀도와 계층을 설계하고, 사장님이 10초 이내에 핵심 상태를 파악할 수 있도록 돕습니다.
* **작업 범위**: `/report` 화면의 지표 카드, 오늘의 요약 3종, 탭 인터랙션 및 복사 기능 구현.
* **주요 UI 컴포넌트**: Metric Card, 오늘의 핵심 요약 3종, Review Risk Card, Reply Draft Card, Warning Badge, Improvement Suggestion Card.
* **연결 화면**: `/report` 리포트 화면.
* **연결 QA ID**: `QA-RP-01~07`.
* **Acceptance Criteria**:
  1. **리포트 생성일시**: "리포트 생성일시: YYYY-MM-DD HH:MM" 표준 양식 적용.
  2. **상단 4대 지표 고정**: 1. 총 분석 리뷰 수, 2. 평균 별점, 3. 주요 부정 키워드 수, 4. 검토 우선 리뷰 수로 상단 지표 카드 고정 매핑.
  3. **지표/배너 격리 규칙**:
     - "반복 불만 TOP 키워드"는 상단 지표 카드 내에 포함되지 않고, [탭 1] 본문의 키워드 칩 또는 TOP 5 영역에만 출력되도록 격리.
     - 생성된 AI 답글 내 민감 표현 감지 건수(가드레일 알림)는 상단 지표 카드가 아닌, [탭 2] 개별 답글 내부 `[민감 표현 감지 - 수동 검수 필수]` 주황색 배지로만 표출되도록 격리.
  4. **요약 3종 시퀀스**: 요약 카드가 전체 상태 요약(카드 1), 부정 키워드 분석(카드 2), 실행 조치 제안(카드 3)의 3층 구조로 출력될 것.
  5. **AI 답글 수동 복사 인터랙션**: 4대 톤 탭 전환을 거쳐 사장님이 직접 "클립보드로 복사하기" 버튼을 눌러 복사해 가는 수동 복사 방식으로만 설계.
* **금지 사항**: 메인 4대 지표 변경 금지, 자동 답변 전송 및 스토어/매장 자동 등록 UI 추가 금지.
* **PR 제출 증빙**: 리포트 화면 탭 1, 2, 3 전환 캡처 및 클립보드 복사 피드백 캡처.

### 4.4. RAI-S2-UX04 — 모바일 탭/리뷰 카드/CTA QA
* **목적**: 모바일 뷰포트에서의 레이아웃 깨짐을 전면 방어하고 좁은 가로 폭에 최적화된 마이크로 UX를 구현합니다.
* **작업 범위**: 모바일 미디어 쿼리, 모바일용 스크롤 필 탭, 텍스트 생략 아코디언 및 카카오 상담 CTA 배치.
* **주요 UI 컴포넌트**: 가로 스크롤 필 탭바, Read More 텍스트 영역, Kakao CTA Banner.
* **연결 화면**: `/`, `/submit`, `/report`.
* **연결 QA ID**: `QA-MB-01~05`, `QA-CT-01~05`.
* **Acceptance Criteria**:
  1. **모바일 해상도 검수**: iPhone SE 375px 및 일반 모바일 390px 뷰포트에서 가로 스크롤바가 발생하거나 텍스트가 카드 밖으로 삐져나가는 깨짐 에러 방지.
  2. **지표 카드 그리드**: 리포트 상단의 메인 4대 지표 카드가 좁은 뷰포트에서 세로 1열 흐름 또는 2열 그리드로 자동 재배치될 것.
  3. **가로 스크롤 필 탭**: report 3개 메인 탭바가 좁은 화면에서 줄바꿈되지 않고 가로 스크롤이 가능한 필(Pill) 디자인으로 렌더링되게 구현.
  4. **긴 리뷰 생략 처리**: 100자 이상 긴 부정 리뷰 카드 노출 시, 화면 높이 확보를 위해 2줄 생략 후 `자세히 보기 ∨` 버튼을 누르면 아코디언 형태로 펼쳐지는 동작 구현.
  5. **카카오 1:1 배너 배치**: `/report` [탭 3] 상세페이지 개선 제안 최하단 배너 영역에 카카오 옐로우 테마의 1:1 상담 안내 CTA 렌더링.
* **금지 사항**: 모바일 화면 최하단 스티키 플로팅 결제/상담 바 고정 금지, 실결제창 모사 및 결제 완료 사칭 UI 추가 금지.
* **PR 제출 증빙**: Chrome DevTools 375px/390px 캡처 및 아코디언 작동 캡처.

### 4.5. RAI-S2-UX05 — 인쇄 레이아웃 QA
* **목적**: 사장님이 리포트 인쇄하기 버튼을 누르거나 Cmd+P를 입력했을 때, A4 용지 기준에 맞춰 읽기 쉬운 리포트 출력 화면을 제공합니다.
* **작업 범위**: `@media print` 내 CSS 마진/패딩 및 display 처리.
* **주요 UI 컴포넌트**: 전체 리포트 출력 영역.
* **연결 화면**: `/report` 리포트 화면.
* **연결 QA ID**: `QA-PR-01~04`.
* **Acceptance Criteria**:
  1. **인쇄 미디어 쿼리 적용**: 브라우저 인쇄 모드 진입 시, 내비게이션 바, 탭 전환 바, 복사 버튼, 상담 CTA 배너 등 불필요한 모든 조작용 UI 요소를 `display: none` 처리.
  2. **순수 리포트 레이아웃**: 스토어 정보, 생성일시, 메인 4대 지표, 3종 요약문, 선택된 본문 결과 등 핵심 보고 내용만 깔끔하게 정렬되도록 구성.
  3. **인쇄 가독성 기준 적용**: 잉크 절약 및 높은 가독성을 위해 `@media print` 내에서 배경은 화이트(`#FFFFFF`), 텍스트는 딤블랙(`#1A1A1A`) 기준으로 적용.
  4. **인쇄 마진 최적화**: 테이블 경계선이 잘려 나가지 않고, 카드의 텍스트가 불필요하게 개행되어 출력 용지가 여러 장으로 늘어지지 않도록 최적화.
* **금지 사항**: 인쇄 화면에서 불필요한 결제 유도 배너, 조작 단추가 그대로 남게 방치 금지.
* **PR 제출 증빙**: Cmd+P 인쇄 대화창 내의 미리보기 화면 캡처.

---

## 5. 덱스 임의 디자인 방지 8대 가이드

서비스의 퀄리티 일관성 및 정책 리스크 제어를 위해 덱스가 독자적으로 스타일이나 구성을 바꾸지 못하도록 엄격히 선언합니다.

1. **컬러/폰트/여백 임의 변경 금지**: `index.css`에 선언된 브랜드 컬러, Pretendard 폰트 위계, 여백 규칙 외의 ad-hoc 스타일링을 임의로 삽입하는 것을 금지합니다.
2. **홈 7대 섹션 순서 변경 금지**: 랜딩 페이지의 흐름은 기획 의도에 맞춰 설계된 순서이므로 덱스가 자체적으로 마크업 순서를 재배치하는 것을 금지합니다.
3. **메인 4대 지표 변경 금지**: 리포트의 메인 지표는 정해진 4대 항목(총 리뷰 수, 평균 별점, 주요 부정 키워드 수, 검토 우선 리뷰 수) 외의 타 항목으로 대체하는 것을 금지합니다.
4. **반복 불만 TOP 키워드의 상단 지표 카드 배치 금지**: 정보 피로도 감소를 위해 해당 키워드 칩 목록은 반드시 [탭 1] 본문 영역에만 노출되어야 합니다.
5. **민감 표현 감지 건수의 상단 지표 카드 배치 금지**: 평판 낙인 효과 방지를 위해 해당 건수는 [탭 2] AI 답글 초안 카드 하단 혹은 보조 배지로만 격리 배치해야 합니다.
6. **모바일 플로팅 CTA 추가 금지**: 사용성을 해치는 모바일 화면 스티키 플로팅 바를 임의로 추가하는 것을 금지합니다.
7. **자동 리뷰 수집/자동 게시처럼 보이는 UI 추가 금지**: 크롤링 지연 혼동 및 API 정책 위반 소지를 방지하기 위해, 오직 수동 CSV 업로드 및 수동 답글 복사 동선만 고수해야 합니다.
8. **금지 표현 노출 금지**: 덱스는 소스코드 및 번들 전반에서 8대 금지어(완벽, 최상, 100%, 전격, 보장, 허위 리뷰, 악성 고객, 블랙컨슈머)가 UI 본문 텍스트에 나타나지 않도록 `grep` 등으로 전수 조사해야 합니다.

---

## 6. ST06C QA 체크리스트 매핑 테이블

덱스는 개발 단계 및 QA 검수 시 다음 매핑 구조를 참조하여 티켓 완료를 검증해야 합니다.

| 개발 티켓 ID | 관련 QA 검수 ID | 대상 화면/기능 |
| :--- | :--- | :--- |
| **`RAI-S2-UX01`** | **`QA-MB-06`**, 디자인 토큰 공통 항목 | 전체 공통 레이아웃, 디자인 시스템 토큰 일치 여부, 최소 터치 영역 |
| **`RAI-S2-UX02`** | **`QA-HM-01~06`**, **`QA-IP-01~07`** | 홈 7대 섹션, CSV 업로드 드롭존 파싱 피드백, 100건 초과 모달, 테스트 샘플 |
| **`RAI-S2-UX03`** | **`QA-RP-01~07`** | 리포트 생성일시, 4대 지표, 부정 키워드 칩 및 민감 배지 격리, 오늘의 요약 3종, AI 답글 수동 복사 |
| **`RAI-S2-UX04`** | **`QA-MB-01~05`**, **`QA-CT-01~05`** | 모바일 SE/390px 텍스트 오버플로우 방지, 가로 스크롤 필 탭, 2줄 아코디언, 카카오 상담 CTA 배너 |
| **`RAI-S2-UX05`** | **`QA-PR-01~04`** | 브라우저 인쇄 미디어 쿼리 필터 적용, 배경 화이트/딤블랙 텍스트, 인쇄 마진 조율 |

---

## 7. 덱스 PR 제출용 6대 증빙 체크리스트

덱스는 S2 구현 완료 후 main 브랜치 풀리퀘스트(PR)를 작성할 때, 아래의 6대 증빙 자료를 PR 본문에 반드시 마크다운으로 표기하여 첨부해야 합니다.

1. **로컬 실행 화면 확인 경로**: 덱스가 로컬 환경에서 테스트하고 렌더링한 URL 정보 명기.
2. **모바일 375px/390px 확인 결과**: 크롬 개발자 도구 뷰포트에서 촬영한 SE 및 일반 모바일 캡처 이미지 첨부.
3. **0건/1건/100건/101건 입력 테스트 결과**: 직접 파일 업로드 및 데이터 파싱 수량별 테스트 로그 캡처 또는 텍스트 피드백 첨부.
4. **금지 표현 grep 검색 결과**: 소스 코드 전반에서 금지어(완벽, 보장, 허위 리뷰 등)를 검색한 터미널 콘솔 텍스트 출력본 첨부.
5. **인쇄 미리보기 확인 결과**: Cmd+P 인쇄 대화창의 미리보기 화면 캡처 이미지 첨부.
6. **수정 및 추가 파일 목록**: S2 범위 내에서 생성/수정된 HTML, CSS, JS 파일들의 구조 목록 요약.

---

## 8. ST08로 넘길 작업 분해 후보

스프린트 2 개발 계획 수립 단계(`ST08`)에서 덱스가 상세 작업 티켓으로 분할하여 일정을 추정하고 개발 준비 범위를 확인할 수 있도록 5대 태스크를 더 세부적인 태스크 단위로 쪼갠 후보군입니다. *(※ 실제 개발 지시문은 아니며 기획적인 분해 가이드라인입니다.)*

### 8.1. `RAI-S2-UX01` 세분화 후보
* **`RAI-S2-UX01-01`**: `index.css` 디자인 토큰 변수 정의 (브랜드 컬러, 폰트 위계, 기본 카드 라운드, 그림자 속성 정의)
* **`RAI-S2-UX01-02`**: 공통 레이아웃 구조 마크업 및 기본 CSS 뼈대 작성 (상단 헤더 내비게이션 바, 하단 풋터 영역 기본 마크업)
* **`RAI-S2-UX01-03`**: 공통 버튼, 카드 컨테이너, 배지용 기본 스타일 시트 작성 및 최소 모바일 터치 규격(`44px` 이상) 일괄 적용

### 8.2. `RAI-S2-UX02` 세분화 후보
* **`RAI-S2-UX02-01`**: 홈 화면(`/`) 퍼블리싱 및 7대 섹션 흐름 마크업 (Hero, 가치 카드, 추천 사장님, 상품 구성 등 카피 일치 적용)
* **`RAI-S2-UX02-02`**: submit 화면(`/submit`) CSV 업로드 드롭존 마크업 및 실시간 파일명/파싱 수량/유효성 체크 배지 인터랙션 구현
* **`RAI-S2-UX02-03`**: 100건 초과 차단 Limit Modal 마크업, 카카오 1:1 상담 문의 배너 연동 및 15건 샘플 데이터 불러오기 기능 구현

### 8.3. `RAI-S2-UX03` 세분화 후보
* **`RAI-S2-UX03-01`**: 리포트 화면(`/report`) 인쇄 일시 헤더 마크업, 상단 4대 지표 카드 고정 매핑 및 오늘의 요약 3종 리스트 카드 구현
* **`RAI-S2-UX03-02`**: 리포트 [탭 1] 본문 분석 결과 퍼블리싱 (검토 우선 리뷰 리스트, 별점 표시, 부정 키워드 칩 목록 격리 노출)
* **`RAI-S2-UX03-03`**: 리포트 [탭 2] AI 답글 초안 카드 마크업 (4대 브랜드 톤 전환 제어, 수동 복사 버튼 및 민감 경고 배지 가드레일 구현)

### 8.4. `RAI-S2-UX04` 세분화 후보
* **`RAI-S2-UX04-01`**: 375px/390px 미디어 쿼리 정의 및 가로 깨짐 방지 뷰포트 디버깅 (전체 반응형 Flex/Grid 흐름 점검)
* **`RAI-S2-UX04-02`**: 모바일용 가로 스크롤 필 탭바 구현 및 긴 리뷰 2줄 제한 / 자세히 보기 확장 인터랙션 구현
* **`RAI-S2-UX04-03`**: 모바일용 카카오 1:1 상담 안내 CTA 배너 배치 및 모바일 전용 하단 스티키 플로팅 바 제거 디버깅

### 8.5. `RAI-S2-UX05` 세분화 후보
* **`RAI-S2-UX05-01`**: `@media print` 인쇄용 스타일 시트 정의 (내비바, 탭바, 카카오 배너, 복사 단추 display: none 일괄 처리)
* **`RAI-S2-UX05-02`**: A4 인쇄 레이아웃 최적화 (흰색 배경, 딤블랙 텍스트 적용 및 불필요한 개행에 의한 여러 장 출력 방지 마진 조율)
